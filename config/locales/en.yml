---
en:
  activemodel:
    errors:
      models:
        convert_to_scheduled:
          attributes:
            scheduled_date:
              not_in_future: must be in the future
        scheduled_to_do:
          attributes:
            scheduled_date:
              not_in_future: must be in the future
        session:
          attributes:
            base:
              invalid_credentials: invalid login credentials
        update_scheduled_date:
          attributes:
            scheduled_date:
              not_in_future: must be in the future
  helpers:
    submit:
      activate_list:
        update: Reactivate
      archive_list:
        update: Archive
      archive_scheduled:
        update: Cancel
      convert_from_scheduled:
        update: Convert From Scheduled
      convert_to_scheduled:
        update: Convert To Scheduled
      list:
        create: Add List
      list_email:
        update: Email Me This List
      rename_list:
        update: Rename List
      scheduled_to_do:
        create: Add Scheduled To-Do
      session:
        create: Log In
      to_do:
        create: Add To-Do
      update_scheduled_date:
        update: Update Scheduled Date
      update_task:
        update: Update Task
  layouts:
    nav_bar:
      archive: Archived Lists
      home: My Lists
      log_out: Log Out
  lists:
    archive:
      title: Archived Lists
    index:
      title: My Lists
  scheduled_to_dos:
    index:
      title: Scheduled To-Dos For %{list}
  simple_form:
    labels:
      session:
        email: Email
        password: Password
    required:
      mark: ''
  to_do:
    states:
      archived: archive
      done: done
      priority: priority
      scheduled: scheduled
      to_do: to do
  to_dos:
    index:
      archive_done: Archive Done
      email_sent: Email Sent!
      rename_list: Rename List
      scheduled_to_dos: Scheduled To-Dos
      sending_email: Sending Email...
    show:
      convert_to_scheduled: Convert To Scheduled
      scheduled_date: Scheduled Date
      state: State
      task: Task
      update_scheduled_date: Update Scheduled Date
      update_task: Update Task
