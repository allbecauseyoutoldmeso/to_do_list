---
en:
  activemodel:
    errors:
      models:
        session:
          attributes:
            base:
              invalid_credentials: Invalid Login Credentials
  helpers:
    submit:
      list:
        create: Add List
        update: Archive
      session:
        create: Log In
      to_do:
        create: Add ToDo
  layouts:
    nav_bar:
      home: My Lists
      log_out: Log Out
  lists:
    index:
      name: Name
      title: My Lists
  simple_form:
    labels:
      session:
        email: Email
        password: Password
    required:
      mark: ''
  to_do:
    states:
      archived: archive
      done: done
      to_do: to do
      urgent: urgent
  to_dos:
    index:
      task: Task
