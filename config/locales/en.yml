---
en:
  activemodel:
    errors:
      models:
        session:
          attributes:
            base:
              invalid_credentials: Invalid Login Credentials
  helpers:
    submit:
      list:
        create: Add List
        update: Archive
      session:
        create: Log In
      to_do:
        create: Add ToDo
  layouts:
    nav_bar:
      archive: Archived Lists
      home: My Lists
      log_out: Log Out
  lists:
    archive:
      reactivate: Reactivate
      title: Archived Lists
    index:
      name: Name
      title: My Lists
  simple_form:
    labels:
      session:
        email: Email
        password: Password
    required:
      mark: ''
  to_do:
    states:
      archived: archive
      done: done
      priority: priority
      to_do: to do
  to_dos:
    index:
      email_list: Email Me This List
      email_sent: Email Sent!
      sending_email: Sending Email...
      task: Task
