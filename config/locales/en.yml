---
en:
  activemodel:
    errors:
      models:
        convert_to_scheduled:
          attributes:
            scheduled_date:
              not_in_future: Must be in the future
        scheduled_to_do:
          attributes:
            scheduled_date:
              not_in_future: Must be in the future
        session:
          attributes:
            base:
              invalid_credentials: Invalid Login Credentials
  helpers:
    submit:
      archive_scheduled:
        update: Cancel
      convert_from_scheduled:
        update: Convert From Scheduled
      convert_to_scheduled:
        update: Convert To Scheduled
      list:
        create: Add List
        update: Archive
      scheduled_to_do:
        create: Add Scheduled ToDo
      session:
        create: Log In
      to_do:
        create: Add ToDo
  layouts:
    nav_bar:
      archive: Archived Lists
      home: My Lists
      log_out: Log Out
  lists:
    archive:
      reactivate: Reactivate
      title: Archived Lists
    index:
      name: Name
      title: My Lists
  scheduled_to_dos:
    index:
      back_to_list: Back to %{list}
      title: Scheduled ToDos for %{list}
  simple_form:
    labels:
      session:
        email: Email
        password: Password
    required:
      mark: ''
  to_do:
    states:
      archived: archive
      done: done
      priority: priority
      scheduled: scheduled
      to_do: to do
  to_dos:
    index:
      archive_done: Archive Done
      email_list: Email Me This List
      email_sent: Email Sent!
      scheduled_to_dos: Scheduled ToDos
      sending_email: Sending Email...
      task: Task
    show:
      all_scheduled_to_dos: All scheduled todos for %{list}
      all_to_dos: All todos for %{list}
      scheduled_date: Scheduled Date
      state: State
      task: Task
