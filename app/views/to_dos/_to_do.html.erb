<%= content_tag(:li) do %>
  <%= link_to(
    to_do.task,
    list_to_do_path(list, to_do),
    class: to_do.state
  ) %>
  <%= render(
    partial: 'update_states/edit',
    locals: { list: list, to_do: to_do }
  ) %>
<% end %>
