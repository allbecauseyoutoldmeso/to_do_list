<%= simple_form_for(
  [list, ArchiveScheduled.new(scheduled_to_do)],
  html: {
    data: {
      action: 'submit->requests#submitForm'
    }
  }
) do |form| %>
  <%= form.submit(class: 'archive-button') %>
<% end %>
