<%= content_tag(:li) do %>
  <%= link_to(archived_list.name, list_to_dos_path(archived_list)) %>
  <%= render(
    partial: 'activate_lists/edit',
    locals: { list: archived_list }
  ) %>
<% end %>
