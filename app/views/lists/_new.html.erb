<%= simple_form_for(
  new_list,
  html: {
    data: {
      action: 'submit->requests#submitForm'
    }
  }
) do |form| %>
  <%= form.input(
    :name,
    label: false,
    input_html: { data: { focus_target: 'focusable' } }
  ) %>
  <%= form.submit %>
<% end %>
