<%= content_tag(:h1, t('.title', list: @list.name)) %>
<%= link_to(
  t('.back_to_list', list: @list.name),
  list_to_dos_path(@list),
  class: 'action-item'
) %>
<%= render(
  partial: 'scheduled_to_dos',
  locals: { list: @list, scheduled_to_do: @scheduled_to_do }
) %>
